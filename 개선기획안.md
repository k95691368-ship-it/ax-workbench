# 개선 기획안 — 배포 후 개선 사이클 기록

1차 배포(https://ax-workbench.pages.dev) 이후, 실제 서비스 사이트를 벤치마킹하며
"사람이 만든 것처럼 자연스러운" 수준까지 반복 개선한 기록이다.

---

## 사이클 1 — 랜딩·카피·공유 미리보기 (2026-07-25)

### 벤치마킹
- **채널톡(channel.io)**: 히어로 아래 수치 기반 신뢰 요소("242,541개 기업", "98% 유지율"),
  "일주일 넘게 걸리던 분석을 5분만에" 식의 **비포/애프터 시간 비교**로 ROI를 보여줌.
- **토스(toss.im)**: 청유형 종결어미("확인해보세요"), 짧은 훅 + 이익 중심 문장, 리듬감 있는
  3연속 대구("한도는 높게, 금리는 낮게, 부담은 적게").

### 진단 (내 사이트와의 차이)
1. 히어로가 설명문 위주 — 행동을 유도하는 청유형·이익 중심 카피가 없다.
2. 신뢰 요소(숫자)가 전혀 없다 — 실사이트는 반드시 수치 스트립이 있다.
3. 비포/애프터가 없다 — "이 도구가 시간을 얼마나 줄이는가"가 한눈에 안 보인다.
4. OG 메타태그가 없어 카톡·메일로 링크 제출 시 미리보기가 비어 보인다.
5. 푸터가 한 줄짜리 — 사이트가 갑자기 끝나는 느낌. 실사이트는 푸터도 정보 구조가 있다.

### 개선 항목
- [x] 히어로 카피 개편: 짧은 훅 + 청유형 CTA("30초 만에 상세페이지부터 만들어 보세요")
- [x] 수치 스트립 추가: 데모 4종의 비포/애프터 시간 비교 카드
- [x] OG/SEO 메타태그 추가 (title, description, og:*)
- [x] 푸터 3열 구조화 (데모 바로가기 / 문서 / 기술 스택)
- [x] 랜딩 마지막에 클로징 CTA 섹션 추가 (실사이트의 "마지막 CTA" 패턴)

---

## 사이클 2 — 상세페이지 생성기 UX 심화 (2026-07-25)

### 벤치마킹
- **미리캔버스(miricanvas.com)**: 설명 대신 즉시 행동 유도("Search Templates"), 결과물의
  활용 장면(모바일 목업)을 함께 보여줌, "Just one click" 식 단순성 강조.
- 실제 커머스 상세페이지는 **모바일 우선**으로 소비됨 — PC 폭 미리보기만으로는 실무 감각과 어긋남.

### 진단
1. 생성 대기 10~20초 동안 화면이 멈춘 느낌 — 진행 단계가 보이지 않는다.
2. 미리보기가 PC 폭 고정 — 실무에서는 모바일 화면으로 먼저 검수한다.
3. 섹션 카피를 개별 복사할 수 없다 — 실무자는 "이 섹션만" 가져다 쓰는 경우가 많다.
4. 재생성 버튼이 없어 결과가 마음에 안 들면 폼으로 시선이 다시 가야 한다.
5. 생성 완료 후 결과 영역으로 시선 유도가 없다.

### 개선 항목
- [x] 생성 진행 단계 표시 (제품 정보 분석 → 섹션 구조 설계 → 카피 작성 → 검수)
- [x] 모바일(375px)/PC 미리보기 토글 — 기본값 모바일
- [x] 섹션별 카피 복사 버튼 + 디자이너 브리프 전체 복사
- [x] 결과 상단 "다시 생성" 버튼
- [x] 생성 완료 시 결과 영역으로 부드러운 스크롤

---

## 사이클 3 — 콘텐츠 팩토리·리스팅 실물감 (2026-07-25)

### 벤치마킹
- **Buffer(buffer.com)**: 채널 11개를 명시적으로 나열해 "채널마다 다르게"를 제품의 핵심
  가치로 제시. "Turn any idea into the perfect post" — 결과물 중심 카피.
- SNS 관리 도구들의 공통 문법: 게시물을 **실제 채널 모양의 목업**으로 미리 보여준다.
  텍스트 덩어리로 보여주면 도구감이 떨어지고, 목업으로 보여주면 결과물이 즉시 와닿는다.

### 진단
1. 콘텐츠 결과가 전부 `<pre>` 텍스트 — 인스타 글인지 블로그 글인지 눈으로 구분이 안 된다.
2. 카드뉴스가 "1장: ..." 텍스트 나열 — 슬라이드라는 실물감이 없다.
3. 영상 스크립트의 타임라인(0-3초...)이 줄글 — 장면 표로 보여야 촬영 대본답다.
4. 글자수가 안 보인다 — 인스타 캡션·상품명은 글자수가 실무 제약이다.
5. 리스팅 상품명 후보에 복사 버튼과 글자수가 없고, AI 결과물이 금칙어 점검과 연결되지 않는다.

### 개선 항목
- [x] 인스타그램 게시물 목업 미리보기 (프로필 헤더 + 이미지 자리 + 캡션 + 해시태그)
- [x] 카드뉴스를 슬라이드 카드 그리드로 시각화
- [x] 유튜브·틱톡 스크립트를 장면 타임라인 표로 렌더
- [x] 콘텐츠·상품명에 글자수 표시
- [x] 리스팅 상품명 후보별 복사 버튼 + 50자 기준 카운터
- [x] AI가 만든 상품명·본문도 금칙어 스캐너로 즉시 재점검 (파이프라인 일관성)

---

## 사이클 4 — 매출 리포트 대시보드화 (2026-07-25)

### 벤치마킹
- **Mixpanel(mixpanel.com)**: "SQL을 몰라도 쓴다" — 비개발자 접근성이 분석 도구의 핵심 가치.
  의사결정 중심("Ground every product decision in customer truth"), 속도 강조.
- 실무 대시보드 공통 문법: 필터는 차트 위 한 줄, 조회 기간이 항상 보임, 리포트는
  공유 가능한 형태로 내보낼 수 있음.

### 진단
1. 채널 필터가 없다 — "스마트스토어만 보면?"이라는 실무 질문에 답할 수 없다.
2. 조회 기간이 화면에 안 보인다 — 지금 보는 숫자가 언제 데이터인지 불명확.
3. AI 리포트를 복사·저장할 수 없다 — 주간회의에 가져가려면 내보내기가 필요하다.
4. 요일별 패턴이 없다 — 유통 실무에서 요일 편차는 발주·CS 인력 배치의 기본 지표.

### 개선 항목
- [x] 채널 필터 칩 (전체/채널별) — 선택 시 모든 통계·차트·리포트 입력이 재계산
- [x] 조회 기간 자동 표시 (데이터의 최소~최대 날짜)
- [x] AI 리포트 마크다운 복사 + .md 다운로드
- [x] 요일별 평균 매출 차트 추가

---

## 사이클 5 — 채용담당자 관점 마감 (2026-07-25)

### 벤치마킹
- 포트폴리오 작성 가이드 공통론(링커리어·캐치·잡코리아 등): 프로젝트 설명은
  **배경 → 역할 → 액션 → 성과** 구조, 지원 공고(JD)와의 직결성이 1순위 평가 기준,
  문제 해결 과정을 보여줄 것, 코드·문서는 외부 링크로 연결할 것.

### 진단
1. 사이트 어디에도 "왜 이 포트폴리오를 만들었는가"가 없다 — 심사자는 맥락 없이 데모만 본다.
2. 공고 담당업무와 데모의 매핑이 홈 카드에 흩어져 있다 — 한 화면 요약이 필요하다.
3. 제작 과정(기획안 → 구현 → 개선 5사이클)이 사이트 밖 문서에만 있다 — 문제 해결
   과정 자체가 어필 포인트인데 심사자에게 보이지 않는다.
4. 키보드 사용자를 위한 스킵 링크와 포커스 표시가 없다.
5. robots.txt가 없다.

### 개선 항목
- [x] /about 제작기 페이지: 공고 분석 → 설계 원칙 → 담당업무 매핑 표 → 기술 선택 이유
      → 품질(테스트·개선 사이클 기록) 구조
- [x] 내비게이션에 "제작기" 추가
- [x] 스킵 링크 + :focus-visible 포커스 링
- [x] robots.txt 추가
- [x] README·기획안 문서에 라이브 URL 반영

---

## 버그 헌트 라운드 — 멀티에이전트 검증 (2026-07-25)

83개 에이전트(탐색 7 + 검증 70)로 38건을 발견, 반박 검증을 통과한 확정 버그를 일괄 수정.

### 수정 완료 (커밋: 버그 수정 라운드)
- CSV 같은 파일 재선택 무반응 (file input value 미초기화)
- 한국 Excel 기본 저장(CP949) CSV 업로드 시 한글 전파 깨짐 → UTF-8 엄격 디코딩 실패 시 EUC-KR 폴백
- 라이브 AI 응답의 필수 필드 누락 시 서버가 무검증 200 → 프론트 crash: 4개 API에 ensureContract 검증 + max_tokens 절단 응답 전면 거부 + 프론트 이중 가드
- 레이트리밋 D1 오류 시 모든 AI API 500 → fail-open
- dev:full 스크립트 고장(wrangler v4 proxy 모드 제거) → build + pages dev dist 방식
- 날짜 표기(2026/07/13 등) 미정규화로 요일 차트 누락·정렬 왜곡 → normalizeDate
- 빈 수량/매출액 셀이 0으로 집계되던 문제 → 누락 처리
- 통화기호(₩)·단위(개) 붙은 셀 통째 누락 → 숫자만 추출
- 음수 매출일의 "-853%" 같은 무의미한 이상 감지 비율 → 비율 생략
- 겹치는 금칙어 중복 집계("변비 치료" 1건이 2건으로) → 긴 표현 우선 dedupe
- 영상 스크립트 파서가 형식 다른 라인을 조용히 유실 → 허용 폭 확대 + 유실 방지
- 클립보드 복사 실패 무반응, 다운로드 파일명 특수문자, twitter 카드 메타 부재, 데모 응답 안내 문구, About 파이프라인 설명 과장 정정

### 알려진 낮은 우선순위 (백로그)
- 데모 모드 샘플이 대표 예시 제품(유산균) 기준 고정 — 배지에 명시로 완화, 프리셋별 샘플은 추후
- 콘텐츠 탭 ARIA 탭 패턴 미완성, 일별 차트 호버 전용 정보(표 뷰가 대체 수단) — 접근성 라운드에서 개선 예정
- og:image 이미지 에셋 부재 (twitter:card summary 텍스트 카드로 대체)

---

## 사이클 6 — 자율 개선 라운드: 3관점 패널 (2026-07-25)

라이브 AI 전환 직후, 채용 어필·엔지니어링 품질·운영/비용 3개 관점의 AI 패널로
개선안 12건을 도출하고 합의 상위(작은 노력·큰 효과)를 구현했다.

### 구현 완료
- [x] **서버측 표시광고 사후검증 전면 적용** — 프론트 /listing에만 있던 금칙어 점검을
      공용 모듈(functions/_lib/adcheck.js, 프론트와 같은 규칙 공유)로 승격.
      상세페이지·콘텐츠·리스팅의 모든 AI 생성 결과를 서버에서 재점검해 ad_check로 반환,
      화면에 "표시광고 자동 점검 통과 / 주의 N건" 배지 표시. 프롬프트 규칙 + 규칙 기반
      사후검증의 이중 방어 구조 완성.
- [x] **운영 메타 표시** — 라이브 생성마다 실측 토큰 사용량(입력/출력)과 추정 비용을
      결과 하단에 표시 (AI 운영·비용 감각 어필).
- [x] **타임아웃 + 재시도 + 우아한 강등** — Claude 호출에 40초 타임아웃, 429/5xx 1회
      자동 재시도. 최종 실패 시 502 에러 대신 예시 결과 + 안내문으로 응답(심사자가
      빈 에러 화면을 볼 확률 제거).
- [x] **전역 일일 예산 캡** — 전 엔드포인트 합산 하루 300회 라이브 생성 한도. 초과 시
      서비스 중단이 아니라 예시 결과로 강등 (비용 폭주 방지 + 데모 연속성).
- [x] React ErrorBoundary(라우트별 리셋), 콘텐츠 생성 max_tokens 16000으로 상향
- [x] 테스트 23 → 26개 (서버측 사후검증 케이스 추가)

### 백로그 (다음 라운드 후보)
- 데모 흐름 GIF/영상 (README + 랜딩) — 화면 녹화 필요, 사용자 작업
- About 페이지 심사자 브리핑 강화 (요건↔구현 체크리스트 + 아키텍처 다이어그램)
- AI 호출 텔레메트리 D1 기록 + 신뢰성/비용 지표 페이지
- Cloudflare Turnstile 봇 방어 (사이트 키 발급 필요)
- 백엔드 callClaudeTool/rateLimit 모킹 테스트

---

## 사이클 7 — 백로그 소화 라운드 (2026-07-25)

- [x] 데모 GIF 제작·README 최상단 배치 — Playwright 자동화 브라우저로 실제 사이트 7장면
      캡처(라이브 AI 생성 포함) 후 GIF 인코딩 (800px, 0.26MB)
- [x] About "심사자용 브리핑" — 공고 요건 체크리스트 표(요건→증명 데모→구현 근거) +
      시스템 구조도(브라우저→Functions→Claude/D1, 폴백·이중 컴플라이언스 주석)
- [x] 운영 텔레메트리 — ai_calls 테이블(D1), 4개 API waitUntil 비동기 기록,
      GET /api/ax/stats 7일 집계, About "실측 운영 지표" 카드. 개인정보 미저장.
- [x] 백엔드 모킹 테스트 11개 추가 (callClaudeTool 재시도/절단 거부, ensureContract,
      rateLimit fail-open) — 테스트 26 → 37개
- [x] Cloudflare Turnstile 봇 방어 — Invisible 위젯 + 서버 siteverify 검증 (모든 AI API,
      시크릿 미설정 시 fail-open, 검증 테스트 5개)

---

## 사이클 8 — 상세페이지 AI 피드백 + 디자인 시안 (2026-07-25, 사용자 요청)

- [x] **AI 피드백 루프**: 생성 결과에 자연어 피드백("더 고급스럽게", "문장 짧게" 등)을 주면
      이전 결과를 컨텍스트로 유지한 채 개선판을 생성. 원클릭 피드백 칩 4종 제공.
- [x] **버전 히스토리**: 피드백마다 버전이 쌓여 칩으로 전환·비교 (최대 6개 유지,
      각 버전에 적용된 피드백을 툴팁으로 표시)
- [x] **디자인 테마 3종**: 클린 그린 / 아이보리 프리미엄 / 비비드 팝 — AI 호출 없이
      미리보기·다운로드 HTML·디자인 브리프에 즉시 반영
- [x] 서버: revision(previous+feedback) 입력 검증·길이 제한, 기존 안전장치(금칙어
      재점검·사용량 표시·레이트리밋·Turnstile) 그대로 적용
- [x] (추가 요청) 디자인 테마 리치화: 단색 → 그라데이션 + 유사 3D 장식 5종
      (클린 그린 / 아이보리 프리미엄 / 비비드 팝 / 글래스 3D / 미드나잇 네온) —
      CSS 전용(이미지 파일 없음), 미리보기·다운로드 HTML 동일 적용
- [x] (추가 요청) "내 이미지" 테마 — 사용자가 올린 사진을 히어로 배경으로 사용.
      브라우저 로컬에서 1400px로 축소·오버레이 자동 적용, 서버 미전송, HTML에 내장

---

## 사이클 9 — 데모 파이프라인화 (2026-07-25)

- [x] **데모 연결 흐름** — 상세페이지 결과에서 "채널 콘텐츠 만들기 →", "상품등록 최적화 →"
      버튼으로 제품 정보가 자동 전달. 데모 5종이 개별 도구가 아니라 하나의 업무 파이프라인으로
      이어짐 (면접 시연 동선: 생성 → 클릭 1번 → 다음 업무)
- [x] **콘텐츠 팩토리 피드백 루프** — 상세페이지와 동일한 "AI 피드백 + 버전 히스토리"를
      콘텐츠 생성에도 적용. 피드백 하나가 생성된 모든 채널에 일관 반영
- [x] **README GIF 2차 갱신** — 테마 전환(글래스 3D·미드나잇 네온)·피드백 박스·채널 목업 등
      10장면 (0.44MB)
- [x] 로컬 QA 자동화: 제품 전달·버전 생성 흐름을 Playwright로 검증 (2건 통과)

---

## 사이클 10 — AX 본질 강화: 대량 처리 + CS 자동 응대 (2026-07-25, 사용자 지시)

"AX의 본질은 판단이 필요 없는 반복 업무의 대체"라는 기준으로 기능을 재설계한 라운드.

- [x] **대량 등록 도우미(/batch)** — 상품 목록(한 줄에 하나, 최대 5개)을 AI 호출 1번으로
      일괄 최적화. 결과 표 + 등록용 CSV 다운로드. 금칙어 검출 상품만 플래그 →
      "사람은 플래그 건만 확인". 공고의 "연 1,000개 상품" 문구 직격.
- [x] **리뷰 자동 응대(/reviews)** — 리뷰 최대 8건 일괄 분류(칭찬/배송/품질/환불·교환/문의)
      + 답변 초안 생성. 건강 이상 호소·법적 클레임 등 판단 필요 건은 **에스컬레이션**
      (AI가 답하지 않고 보수적 초안 + "담당자 확인 필요" 플래그) — "반복은 대체하고
      판단은 남긴다"를 기능으로 구현.
- [x] 두 기능 모두 기존 안전장치 상속(Turnstile·레이트리밋·일일 예산 캡·금칙어 재점검·
      토큰 표시·텔레메트리·데모 폴백)
- [x] 네비·홈 카드·About 요건 매핑 갱신, 테스트 42 → 45개
- [x] 로컬 QA: 배치 표 5행·에스컬레이션 정확히 1건(건강 이상 리뷰만) 검증,
      데모 분류 규칙 오분류 2건 발견·수정
